name: modernlogintute
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'  # Updated to support future Dart versions

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8  # Updated to latest version
  firebase_core: ^3.5.0    # Latest version from flutter pub outdated
  firebase_auth: ^5.3.0    # Latest version from flutter pub outdated

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0    # Updated to latest linting rules

flutter:
  uses-material-design: true

  assets:
    - lib/images/
